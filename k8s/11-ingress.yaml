# Ingress para Frontend (Kong)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms-invest-portal-ingress
  namespace: ms-invest-portal
  labels:
    app: ms-invest-portal
    external: "false"
    priority: "5"
spec:
  ingressClassName: kong
  rules:
  - host: invest-portal.hom-lionx.com.br
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ms-invest-portal
            port:
              number: 80
  tls:
  - hosts:
    - invest-portal.hom-lionx.com.br
    secretName: ms-invest-portal
