// DEPRECATED: Este arquivo não é mais usado
// O frontend agora usa apiClient para conectar ao backend PostgreSQL no Kubernetes
// Mantido apenas para compatibilidade com tipos

throw new Error(
  'Supabase client não está mais disponível. Use apiClient em @/lib/apiClient'
);

export const supabase = null as any;
